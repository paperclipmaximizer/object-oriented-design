are used so that we can assign values to attributes during instantion
```java
this.numberOfLegs = legs;
```
A subclass constructor must call its superclass's constructor if the superclass has an explicit constructor. 
